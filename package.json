{
  "name": "autofeed-backend-v2",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API built with Hono and Wrangler.",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "check": "npm run typegen && npm run typecheck",
    "deploy": "wrangler deploy"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "hono"
  ],
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.84.1"
  }
}
